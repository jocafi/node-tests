{
  "name": "firebase-utilities",
  "version": "1.0.0",
  "description": "The objective of this project is to create firebase utilities such as csv to json converters, importers for both Firebase models \"Firebase Realtime Database\" and \"Cloud Firestore\".",
  "author": "Joao Araujo (jocafi)",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/app.js",
  "scripts": {
    "build-ts": "tsc",
    "start": "tsc && node dist/app.js",
    "debug": "tsc && node --nolazy --inspect-brk=9229 dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "tslint-fix": "tslint --fix -c tslint.json -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/dotenv": "^4.0.2"
  }
}
